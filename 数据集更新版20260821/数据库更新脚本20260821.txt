--更新主网设备表错误的设备类型：
update "EQUIP"."JBS_ZWEQUIPINFO" set EQUIP_TYPE='1321'  where  "EQUIP_TYPE" = '1312'

--JBS_PWREAL根据"TRAN_ID","DATA_DATE"去重：
DELETE FROM "EQUIP"."JBS_PWREAL"
WHERE ROWID NOT IN (
    SELECT MIN(ROWID)
    FROM "EQUIP"."JBS_PWREAL"
    GROUP BY "TRAN_ID","DATA_DATE"
);

--其他sql
delete "EQUIP"."JBS_PWTERMINAL" where ID='TMP00062787';

update "EQUIP"."JBS_ZWTERMINAL" set CONNECTIVITYNODE_ID ='109000054006023' where "EQUIP_ID" ='TMP00048726';

"
DELETE FROM "EQUIP"."JBS_PWTERMINAL" 
WHERE "ID" = 'TMP00063385';

